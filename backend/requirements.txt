# --- Web framework ---
fastapi>=0.115,<0.117
uvicorn[standard]>=0.32,<0.34
python-multipart>=0.0.20,<0.1

# --- Config & validation ---
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0,<2

# --- Vector store & embeddings ---
# (NumPy-backed local vector store; no separate vector DB needed.)
sentence-transformers==3.0.1
transformers==4.41.2
torch==2.3.1
torchvision==0.18.1

# --- LLM (Gemini, new unified SDK) ---
google-genai>=1.0,<2

# --- PDF ingestion & generation ---
pdfplumber>=0.11,<0.12
pypdf>=5.1,<6
reportlab>=4.2,<5

# --- OCR for scanned PDFs & image uploads (English + Hindi) ---
easyocr>=1.7,<2
pymupdf>=1.24,<2
Pillow>=10,<12

# --- HTTP & utilities ---
httpx>=0.27,<0.29
tqdm>=4.66,<5
numpy>=1.26,<3
